import type { BlendMode,Layer } from './artwork';import type { GeneratorRegistry } from './generator-registry';import type { PointerState,RenderQuality,RenderContext } from '../types';import { mulberry32 } from '../random';import { destroyGenerator,initializeGenerator } from './generator-lifecycle';
export interface LayerSurface{canvas:CanvasImageSource;ctx:CanvasRenderingContext2D;resize(width:number,height:number):void;destroy():void;}
export type SurfaceFactory=()=>LayerSurface;
export interface CompositionFrame{width:number;height:number;pixelWidth:number;pixelHeight:number;dpr:number;time:number;delta:number;frame:number;pointer:PointerState;quality:RenderQuality;layerQualities?:ReadonlyMap<string,RenderQuality>;background:string;transparent?:boolean;}
export interface LayerMetric{layerId:string;milliseconds:number;}
type Entry={surface:LayerSurface;state:unknown;initialized:boolean;width:number;height:number};
export const canvasBlendMode=(mode:BlendMode):GlobalCompositeOperation=>mode==='normal'?'source-over':mode;
export function browserSurfaceFactory():LayerSurface{const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d',{alpha:true})!;return{canvas,ctx,resize:(w,h)=>{canvas.width=w;canvas.height=h;},destroy:()=>{canvas.width=1;canvas.height=1;}};}
export class Compositor{readonly metrics=new Map<string,LayerMetric>();readonly #entries=new Map<string,Entry>();constructor(private readonly registry:GeneratorRegistry,private readonly factory:SurfaceFactory=browserSurfaceFactory,private readonly clock:()=>number=()=>performance.now()){}render(target:CanvasRenderingContext2D,layers:Layer[],soloId:string|null,frame:CompositionFrame){const persistentVisible=new Set(layers.filter(l=>l.visible).map(l=>l.id));for(const[id,entry]of this.#entries)if(!persistentVisible.has(id)){const layer=layers.find(l=>l.id===id),generator=layer&&this.registry.get(layer.generatorId);if(generator)destroyGenerator(generator,entry.state);entry.surface.destroy();this.#entries.delete(id);this.metrics.delete(id);}target.save();target.setTransform(1,0,0,1,0,0);target.globalCompositeOperation='source-over';target.globalAlpha=1;if(!frame.transparent){target.fillStyle=frame.background;target.fillRect(0,0,frame.pixelWidth,frame.pixelHeight);}else target.clearRect(0,0,frame.pixelWidth,frame.pixelHeight);const renderable=layers.filter(l=>l.visible&&(!soloId||l.id===soloId)).sort((a,b)=>a.order-b.order);for(const layer of renderable){const generator=this.registry.get(layer.generatorId);if(!generator||generator.renderer!=='canvas2d')continue;let entry=this.#entries.get(layer.id);if(!entry){entry={surface:this.factory(),state:undefined,initialized:false,width:0,height:0};this.#entries.set(layer.id,entry);}if(entry.width!==frame.pixelWidth||entry.height!==frame.pixelHeight){entry.surface.resize(frame.pixelWidth,frame.pixelHeight);entry.width=frame.pixelWidth;entry.height=frame.pixelHeight;entry.initialized=false;}const layerCtx=entry.surface.ctx;layerCtx.save();layerCtx.setTransform(1,0,0,1,0,0);if((generator.id==='particles'||generator.id==='boids')&&entry.initialized){layerCtx.globalCompositeOperation='destination-out';layerCtx.fillStyle=`rgba(0,0,0,${Math.max(.025,Number(layer.parameters.trail))})`;layerCtx.fillRect(0,0,frame.pixelWidth,frame.pixelHeight);layerCtx.globalCompositeOperation='source-over';}else layerCtx.clearRect(0,0,frame.pixelWidth,frame.pixelHeight);layerCtx.setTransform(frame.dpr,0,0,frame.dpr,0,0);const context:RenderContext={renderer:'canvas2d',ctx:layerCtx,width:frame.width,height:frame.height,time:frame.time,delta:frame.delta,frame:frame.frame,seed:layer.seed,pointer:frame.pointer,quality:frame.layerQualities?.get(layer.id)??frame.quality,params:layer.parameters,palette:layer.palette,random:mulberry32(layer.seed)};if(!entry.initialized){entry.state=initializeGenerator(generator,context);entry.initialized=true;}const start=this.clock();generator.render(context,entry.state);this.metrics.set(layer.id,{layerId:layer.id,milliseconds:Math.max(0,this.clock()-start)});layerCtx.restore();target.globalAlpha=layer.opacity;target.globalCompositeOperation=canvasBlendMode(layer.blendMode);target.drawImage(entry.surface.canvas,0,0,frame.pixelWidth,frame.pixelHeight);}target.globalAlpha=1;target.globalCompositeOperation='source-over';target.restore();return target;}command(layer:Layer,command:string,context:RenderContext){const entry=this.#entries.get(layer.id),generator=this.registry.get(layer.generatorId);if(entry?.initialized&&generator?.command)entry.state=generator.command(command,context,entry.state);return entry?.state;}invalidate(layerId:string){const entry=this.#entries.get(layerId);if(entry)entry.initialized=false;}destroy(){for(const entry of this.#entries.values())entry.surface.destroy();this.#entries.clear();this.metrics.clear();}}
